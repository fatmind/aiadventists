# Reef 与 Team3 的 RSI 之路对比（2026-09-14 重写）

> 大白话版。四个部分：Reef 是什么 → RSI 到底是怎么回事 → Team3 具体怎么做、局限在哪 → 最后批判 Reef。

---

## 一、先介绍 Reef

Reef 是个开源项目，目标是把"让智能体越用越强"这件事做成一套基础设施。它的核心闭环是四步：跑任务 → 打分 → 把分数和对话记录喂给一个优化方法 → 产出新候选（新 prompt 或新权重）· 用一套固定任务对跑 → 赢了才上线。

它分两条线：

| 路线 | 改什么 | 需要什么 |
|---|---|---|
| 训权重（RL） | 模型内部参数 | GPU、判分器 |
| 改 harness（prompt / skill） | 给模型的说明书 | 不用 GPU |

### Reef 用在什么场景

分两条线：训权重要 GPU，改 harness 不用。下面拿"改 harness"这条线说，因为它自己的教程 demo 最简单，也最能说明它到底是怎么转的。

### 它自己的 demo 是怎么跑的

Reef 的教程（evolve-your-harness）用三道只有标准答案的编程题做实验：100000 以内质数个数、算 fib(90)、算一个 CSV 的中位数。判分很死板——**要求把最终答案"单独一行、就一个整数"打出来**，最后一行不是那个整数就得 0 分。

其中一道题长这样：

```text
[fib] With fib(1)=1 and fib(2)=1, compute fib(90) exactly
(memoize, do not approximate). Reply with the value as a plain
integer alone on the last line.
```

标准答案是 `2880067194370816120`。

模型做这道题时，很容易答一大段推理，最后写一句"所以答案是 2880067194370816120。"——注意，整数不是"单独一行"，而是跟在"所以答案是"后面，于是判分给 0 分。这次失败就带着分数和反馈记下来了：

```bash
reef-pi report --score 0 --feedback "missed the empty-token case"
```

失败触发了"让模型给自己提改进"。Reef 把自己这些失败请求、当前技能、反馈拼成一段提示词发给大模型，大模型读后提出一个技能改动，原样返回一个 JSON：

```json
{"id": "direct-answer", "name": "skill",
 "config": {"name": "direct-answer",
  "text": "# direct-answer\n\nEnforces answers to be exact plain integers without additional text.\nFor numerical problems requiring precise values, this skill ensures the response\ncontains only the final integer result on the last line."}}
```

这个改动要不要接受，Reef 用那三道题各跑一遍——"当前技能"跑一遍、"新技能"跑一遍，一题一题对着比。新技能让之前 0 分的那道题变 1 分、另外两道没变差，才发布上线。

### Reef 的效果

这套 demo 在真实机子上跑出过结果：Qwen3-8B 这个模型就是靠这条 `direct-answer` 技能，把原来"答一大段、整数不单独一行"的毛病，修成了"直接答一个整数单独一行"。

### 这套机制的核心：回执号

要看懂这个设计，得先看到 Reef 的一个天生短板（后面批判还会再提一次）：**它只是个代理，看不见"任务"，只看得见一次次单独的请求。但打分从来都是任务级别的——一个任务跑完，只给一个分。** 这两件事一开始就对不上。

拿上面的 `[fib]` 那道题说：模型答这道题时调了很多次（读题意、算、写答案），Reef 把每次调用都记成一条记录；可最后那个 0 分，是整个任务打的。问题来了——**这个 0 分，到底指这几次调用里的哪几条？**

最省事的做法是按时间猜：取"做这道题那几分钟前后的记录"当成这次任务的。可一旦并行跑好几个任务、或者分数晚到一会儿，就猜错了——这个失败会被记到隔壁任务头上，反思模型去学了一段跟 fib 毫不相干的失败，越学越歪。

所以 Reef 的选择是：**每次调用返回时发一个编号（回执号），打分时必须报上这个编号指回具体记录。** 这样那个 0 分精确落在 fib 它自己的那几条调用上，反思模型学到的才真是"fib 因为最后一行不是单独整数而失败"这个因果，而不是隔壁任务的噪声。

说白了：**回执号是 Reef 对自己"看不见任务"这个短板的补偿——用显式编号，把"任务级的分"绑到"调用级的记录"上。** 也正因为如此，用 Reef 的每个 agent 都得额外装一层包装壳：任务从哪到哪，还得客户端自己记好、再报给 Reef 去对号。

## 二、从本质上说，RSI 到底是怎么回事，我们为什么想走这条路

### RSI 的本质

把"自迭代"这个词放一边，先看它到底在做什么。

普通智能体是"人给它指定任务，它去完成"。问题来了：这是台机器，怎么让它变强？老办法是靠人把知识、规则一点一点写进去。但这条路的瓶颈很明显：**人写不完所有场景，而且贵。**

RSI 想走另一条路：**让智能体自己从跑过的任务里，发现哪里不行、哪里能改，然后自己改自己。**

### 为什么今天敢走这条路

核心原因只有一个：**我们把智能当成一种生产资料，而智能这种生产资料，是无限的、廉价的。**

- **无限**：它可以一直跑、可以同时开几百甚至几千个任务，没有人的体力和工时上限。
- **廉价**：比起招人、组团队，多调一次模型、多跑一轮实验，边际成本低得多。

所以我们的赌注是：既然生产智能的代价这么低、数量又不受限，我们就能**大量地把它投入到"找自己的毛病、改自己"这件事上**。用无限便宜的生产资料，去换"自我迭代、自我改善"的空间和机会。这就是我们想走 RSI 的原因。

## 三、反过来介绍 Team3：我们具体怎么做，以及局限在哪

### Team3 是什么

**Team3 是我们自己的一个 harness 系统**，它把"Arch 规划、Dev 开发、UAT 验收"串成一条流水线，让一群智能体分工协作地干活。

### 我们在 Team3 上怎么做 RSI

路子是：

1. **信号来自真实运行的偏差**：智能体在真实项目里跑，凡是偏离理想路径的地方，就记一条经验（experience）人类拍板的决策（decision）。信号来自真实开发，不是人编的。
2. **定期提炼成问题**：从一段时间的经验/决策里，提取出"harness 哪里没拦住"的问题 + "人类偏好"，每个问题都带证据链。
3. **来分析根因、改 harness**：看问题在 prompt、脚本、上下文还是环境，找一个能覆盖一类问题的改法，不零碎加规则。
4. **回归验证没改坏**：拿一个固定干净项目从头跑一遍，确认流程没被搞坏、效率没退化。
5. **闭环靠未来项目**：改对没改对，靠后续真实项目里"相类似的问题不再出现"来渐进确认。

### 一个真实例子：跨 feature 的隐性契约

我们有个项目叫 lottery-messi（做彩票赛事预测）。团队在做模块 2 时，Dev 在真实的开发里撞到了一个跨模块的隐性契约，他的经验记录里原样是这样写的：

```md
## 2026-06-29 | lottery-messi | dev | 经验
Feature #4 把"伤情校验"落地成模板句识别，用正则去认「伤停/缺阵、复出/伤愈、首发/出场」这几类固定句式。
Feature #5 的 mock:seed 用 LLM 生成数据，它的 prompt 必须产出 {球员} 因伤缺阵 / {球员} 伤愈复出
这类句式，否则 mock:validate 那道"按球员跨日期核对伤停状态是否一致"的校验就认不出来。
→ 隐性契约：#4↔#5，#5 实现时务必把该句式写进 prompt 约束。
```

当时 Dev 写 Feature #5 的 mock:seed 时，prompt 长这样（**没有得到句式约束**）：

```js
// src/module_2/mock/seed.mjs
const PROMPT = `
你是足球赛事数据模拟器。请为明天的一场比赛生成若干球员的伤病信息。
要求：输出 JSON 数组，每个元素包含球员名、球队、伤病描述。
伤病可以虚构，但要符合足球常识。
`;
```

结果模型产出的数据是这样的：

```json
[
  { "player": "梅西", "team": "巴塞罗那",   "note": "肌肉不适，恐怕缺阵" },
  { "player": "哈兰德", "team": "曼城",      "note": "恢复训练，可能复出" }
]
```

问题在这：`mock:validate` 里有一道校验，要按球员跨日期核对伤停状态前后是否一致，它靠正则去认「因伤缺阵」「伤愈复出」这些**固定句式**；而模型吐的是"恐怕缺阵""可能复出"这种变体——正则认不出来，这道校验就当"没有伤情"放过去了，等于名存实亡。而这个句式是由 Feature #4 那边的实现方式决定的，两边没对齐，就是这次踩的坑。

改进后的 mock:seed prompt（把句式硬约束写进去）：

```js
const PROMPT = `
你是足球赛事数据模拟器。请为明天的一场比赛生成球员伤病信息。
硬性约束（违反即记录作废）：
1. 输出 JSON 数组，每个元素一个球员。
2. 伤病状态必须二选一用固定句式表达，不得改写为"恐缺阵""或将复出""状态成疑"等任何变体：
   - 缺阵：{球员} 因伤缺阵
   - 复出：{球员} 伤愈复出
3. 字段里若不含上述句式之一，该条记录作废，需重新生成。
`;
```

同时，改进点把这个隐性契约回写进了 Feature #5 的 spec 的验收标准里，让它在仓库里成为硬约束、任何人下游 session 都能读到，而不再只存于 Dev 个人经验里：

```md
### 跨 feature 契约（Arch 验收时回写，不得遗漏）
- [Feature #4] 伤情校验 = 模板句识别，正则只认三类句式：
  缺阵「{球员} 因伤缺阵」；复出「{球员} 伤愈复出」；出场「{球员} 首发/出场」。
- [Feature #5] mock:seed 的 LLM prompt 必须产出与 #4 完全相同的句式，
  否则 mock:validate 那道跨日期核对状态的校验就认不出来。#4↔#5 为硬性依赖，列入本 feature 验收标准。
```

### 这个例子的效果

没有这次改进前，"Feature #4 的句式"这个信息靠在 Dev 记忆里、赌下一个 session 恰好读到；改完以后它进了 spec，成了写死的硬约束，哪怕换一个全新的 session 来做下游 feature，读 spec 就能读到，不会再漏。像这样一个问题，从真实项目的踩坑记录里提炼出明确的改动点，就是 Team3 自我改善的每一步。

### 我们的局限：没有快照能力

我们缺少的是"快照"能力。

什么叫快照？就是把"出问题那一次的环境、代码、数据、对话"完整冻住、定格下来。有了它，我们就能在完全一样的场景里，**只改那一个变量，单独地测**"这个修复到底有没有用"。

我们没有这个能力，所以做不到"单变量验证修复"。于是我们把这件事转嫁成另一种办法：

- 改完以后，用回归跑一个固定项目，验证"基本流程没被搞坏"——这就是验证"这个修复方向对不对"的底线。
- 至于"这个修复到底治没治好那个根因"，我们不立刻确认，而是把它放给未来——靠以后真实开发过程里，看相类似的问题还会不会再次出现。

### 为什么接受这个局限

因为它的方向是往好的走的：回归至少能拦住"方向性改坏"这种事，而"治没治好的最终确认"本来就需要长时间尺度和真实场景，强求一条固定项目立刻证出来，反而会制造误报。这是在我们现有条件下能落地的现实选择。

## 四、批判 Reef：它是个概念和 demo，落不了地

把 Reef 和我们自己做的事摆在一起，能看到它的几个问题：

- 问题一：它只是代理，看不见任务

Reef 是推理接口上的一个代理，它只处理一次次单独的请求，看不见"任务"这条完整的东西。"任务从哪开始到哪结束"这个信息只存在于智能体客户端那边，代理认不出来。所以它只能靠发回执号、让客户端自己记账，结果每个用它的项目都得额外装一层包装壳来弥补。

- 问题二：环境快照它也没做，只是绕开

它不负责恢复历史环境，而是让用户自己把环境写死成"任务包"（一个 Dockerfile + 判分脚本）。对真实复杂项目来说，环境复原恰恰是最难、最该基础设施解决的部分，Reef 却把它整个丢回给用户。

- 问题三："统一自迭代"只是概念

它声称把"训权重"和"改 harness"统一进一个框架，实际两条线的学习核心各是一整套独立的东西，真正共享的只有很薄的一层（记对话、挂分数、版本化上线）。概念上很完整，实现的统一只到记账为止。

- 问题四：它解决"怎么做"，却回避了"做什么"

它有一套精巧的机制在运作，但你要喂给它的评测、环境、任务，全得你自己准备。也就是说，框架本身是个空壳，真正决定成败的部分它全留给了用户。